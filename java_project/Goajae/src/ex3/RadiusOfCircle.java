package ex3;
import java.lang.Math;

public class RadiusOfCircle {

	public double circle1(double r) {	
		
		return 2*Math.PI*r;
	}
	
	public double circle2(double r) {
		
		
		return Math.PI*Math.pow(r,2);	//(r*r)을 Math.pow(,)로 표현가능
	
}
}
